"use strict";

/**
 *
 * the app.slides module provides a controller that helps with
 * content that is broken into slides.
 * 
 */

angular.module("app.slides", []);

module.exports.SlideController = require("./controller");
